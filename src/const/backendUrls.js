const authUrls = {
  login: 'auth/signin/',
  register: 'auth/signup/',
  refreshToken: 'auth/refresh-token/',
  profile: 'auth/profile/',
};


const backendUrls = {
  auth: authUrls,
};

export default backendUrls;
